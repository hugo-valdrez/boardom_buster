to do:
-> vectorized dataset names, to quick search + possibly remove games with the same name (Catan vs Catan expansions, dont want to recommend only catan expansions).
-> the user might want to change the weights of the recommendation system.
-> encode the game description and use it as a vector to test similarity
-> add a field regarding complexity (iirc it was the weight on the bgg dataset)
-> maybe used the min age as weight?


-> use a "golden set" to tune the weights.